package vermittler;

import java.rmi.Naming;
import java.rmi.RemoteException;
import java.rmi.registry.LocateRegistry;
import java.rmi.registry.Registry;
import java.util.ArrayList;

import application.MainScreenInterface;
import interfaces.Observer;
import rmi.interfaces.MitarbeiterRemote;

public class Vermittler extends java.rmi.server.UnicastRemoteObject {

	private ArrayList<String> usersGUI;
	private ArrayList<String> usersToene;
	
	public Vermittler() throws RemoteException {
		super();
		usersGUI = new ArrayList<>();
		usersToene = new ArrayList<>();
	}
	
	public void start() {
		
		try {
			
			Registry registry = LocateRegistry.createRegistry(1099);
			
			while ( true ) {
				
				Thread.sleep(5000);
				
				final String[] services = registry.list();
				for ( String service : services ) {
					
					if ( !this.usersGUI.contains(service) ) {
						System.out.println("GUI: " + service);
						this.usersGUI.add(service);
					
					}
				
				}
				
			}
			//ButtonInterface bi = (ButtonInterface) registry.lookup("button");
			//bi.register(this);
			
		} catch (Exception e) {
			System.out.println(e);
		}
	}
	
	public static void main(String[] args) throws RemoteException {
		Vermittler s = new Vermittler();
		s.start();
}


	
}
